/**
 * پارامترهای قطعی سیستم — همه‌جا از اینجا خوانده می‌شوند تا «کار قطعی» از
 * تصمیم LLM جدا بماند (اصل §6 نقشه‌راه). تغییر آستانه/وزن/سقف فقط اینجا.
 */

import type { ChannelKey } from "./store/types";

/* ── شهر و بازارهای هدف (ICP) ─────────────────────────────── */

export const DEFAULT_CITY = "تهران";

export type Market = {
  id: string;
  title: string;
  /** اولویت پایین‌تر = مهم‌تر (۱ بالاترین اولویت) */
  priority: number;
  /** عبارت‌های جست‌وجو برای Google Places Text Search */
  queryTerms: string[];
  /**
   * عبارت‌های ویژه‌ی «دفتر در شهر هدف» — برای بازارهایی که خودِ کسب‌وکار
   * ممکن است بیرون شهر باشد ولی دفتر مرکزی/فروش در شهر داشته باشد.
   *
   * فقط بازار صنعتی این را دارد: کارخانه‌ی ساوه یا کاشان دفتر تهران دارد و
   * قرار حضوری همان‌جاست. «دفتر مرکزی کلینیک زیبایی» بی‌معنی است، پس بقیه‌ی
   * بازارها این فیلد را نمی‌گیرند.
   *
   * سمت OSM چیزی لازم نیست: مرز Overpass همان شهر است و `office=company`
   * از قبل در تگ‌های بازار صنعتی هست — دفتر تهران خودبه‌خود پیدا می‌شود.
   * شکاف فقط سمت جست‌وجوی وب (Tavily) بود.
   */
  officeTerms?: string[];
  /** تگ‌های OpenStreetMap (Overpass) — منبع پیش‌فرض کشف (رایگان، بدون کلید) */
  osmTags: string[];
};

export const MARKETS: Market[] = [
  {
    id: "industrial",
    title: "کارخانجات و گروه‌های صنعتی",
    priority: 1, // اولویت بالا: ارزش قرارداد بالاتر، ضعف دیجیتال رایج
    queryTerms: ["کارخانه", "شرکت تولیدی", "مجتمع صنعتی", "گروه صنعتی"],
    officeTerms: [
      "دفتر مرکزی کارخانه",
      "دفتر فروش کارخانه",
      "دفتر مرکزی گروه صنعتی",
      "نمایندگی صنایع",
    ],
    osmTags: ["man_made=works", "office=company", "industrial", "craft", "office=industrial"],
  },
  {
    id: "beauty_clinic",
    title: "کلینیک‌های زیبایی و دندان‌پزشکی",
    priority: 2,
    queryTerms: ["کلینیک زیبایی", "کلینیک پوست و مو", "دندان‌پزشکی", "مطب زیبایی"],
    osmTags: [
      "amenity=clinic",
      "amenity=dentist",
      "amenity=doctors",
      "shop=beauty",
      "shop=hairdresser",
      "healthcare=clinic",
    ],
  },
  {
    id: "traditional_highrev",
    title: "کسب‌وکارهای سنتی پردرآمد",
    priority: 3,
    queryTerms: ["فروشگاه", "شرکت پخش", "بازرگانی", "تولیدی پوشاک"],
    osmTags: ["shop=jewelry", "shop=furniture", "shop=clothes", "shop=car", "shop=wholesale"],
  },
  {
    id: "general_ai_fit",
    title: "هر بیزینسی که با AI مشکلاتش حل می‌شود",
    priority: 4,
    queryTerms: ["شرکت خدماتی", "آموزشگاه", "املاک", "مرکز درمانی"],
    osmTags: ["office=estate_agent", "amenity=school", "office=educational_institution", "office=company"],
  },
];

export function getMarket(id: string): Market | undefined {
  return MARKETS.find((m) => m.id === id);
}

/** شناسه‌ی ویژه‌ی «همه‌ی بازارها با هم» (کشف ترکیبی) */
export const ALL_MARKET_ID = "all";

/** آیا این کمپین همه‌ی بازارها را با هم هدف می‌گیرد؟ */
export function isAllMarkets(id: string): boolean {
  return id === ALL_MARKET_ID;
}

/** برچسب نمایشی بازار (شامل حالت ترکیبی) */
export function marketLabel(id: string): string {
  if (isAllMarkets(id)) return "همه‌ی بازارها (ترکیبی)";
  return getMarket(id)?.title ?? id;
}

/** اجتماع (union) تگ‌های OSM همه‌ی بازارها — برای کشف ترکیبی */
export function combinedOsmTags(): string[] {
  return Array.from(new Set(MARKETS.flatMap((m) => m.osmTags)));
}

/** همه‌ی عبارت‌های جست‌وجوی یک بازار (عادی + عبارت‌های دفتر شهر) */
function allTermsOf(m: Market): string[] {
  return [...m.queryTerms, ...(m.officeTerms ?? [])];
}

/**
 * اجتماع عبارت‌های جست‌وجوی همه‌ی بازارها — **چرخشی (round-robin)**، نه پشت‌سرهم.
 *
 * چرا چرخش اینجا و نه در web-search: بودجه‌ی Tavily (۸ فراخوان) خیلی کمتر از
 * تعداد کل عبارت‌هاست، پس فقط ابتدای این فهرست واقعاً جست‌وجو می‌شود. اگر
 * بازارها پشت‌سرهم بیایند، همه‌ی بودجه صرف بازار اول (صنعتی) می‌شود و کلینیک
 * و بقیه هیچ‌وقت نوبتشان نمی‌رسد.
 *
 * قبلاً این کار در `interleaveTerms(terms, groupSize = 4)` انجام می‌شد که فرض
 * می‌کرد **هر بازار دقیقاً ۴ عبارت دارد**. با اضافه‌شدن `officeTerms` به بازار
 * صنعتی (۸ عبارت) آن فرض می‌شکست و بی‌سروصدا بازارها را نامتوازن می‌کرد. اینجا
 * مرز بازارها معلوم است، پس چرخش درست انجام می‌شود.
 */
export function combinedQueryTerms(): string[] {
  const groups = MARKETS.map(allTermsOf);
  const longest = Math.max(0, ...groups.map((g) => g.length));
  const out: string[] = [];
  for (let i = 0; i < longest; i++) {
    for (const g of groups) {
      if (g[i]) out.push(g[i]);
    }
  }
  return Array.from(new Set(out));
}

/** تگ‌های OSM متناسب با بازار (یا ترکیبی) */
export function osmTagsFor(marketId: string): string[] {
  return isAllMarkets(marketId) ? combinedOsmTags() : getMarket(marketId)?.osmTags ?? [];
}

/** عبارت‌های جست‌وجوی متناسب با بازار (یا ترکیبی) — شامل عبارت‌های دفتر شهر */
export function queryTermsFor(marketId: string): string[] {
  if (isAllMarkets(marketId)) return combinedQueryTerms();
  const m = getMarket(marketId);
  return m ? allTermsOf(m) : [marketId];
}

/** بازار پیش‌فرض کمپین جدید — ترکیبی (همه با هم) */
export const DEFAULT_MARKET_ID = ALL_MARKET_ID;

/* ── سقف‌های پایلوت ────────────────────────────────────────── */

export const LIMITS = {
  dailyDiscovery: 20,
  dailyMessage: 5,
  followUp: 1,
} as const;

/* ── سقف مصرف مدل ─────────────────────────────────────────── */

/**
 * سقف واقعی مصرف سهمیه — تنها جایی که تا امروز هیچ گاردی نداشت.
 *
 * وضعیت قبلی: زنجیره‌ی ۳ مدلِ `runAgentText` **ضربدر** حلقه‌ی ۲ تلاشی JSON در
 * `runAgentJSON` = تا ۶ درخواست upstream برای یک فراخوان منطقی. روی لیدی که یک
 * دور بازنویسی بخورد (۹ فراخوان ایجنت)، سقف نظری ۵۴ درخواست بود — یعنی **یک
 * لید می‌توانست کل سهمیه‌ی روز را بسوزاند**، در حالی که دروازه‌ی توان مالی و
 * قفل همزمانی و بودجه‌ی کشف همه سفت بسته بودند. گارد در جای اشتباه بود.
 */
export const AI_LIMITS = {
  /**
   * سقف کل درخواست upstream برای یک فراخوان منطقی ایجنت.
   *
   * ۳ یعنی: یا مدل اصلی + دو جایگزین، یا مدل اصلی + یک تلاش دوم JSON + یک
   * جایگزین. هر ترکیبی، ولی هرگز بیشتر از ۳ تا.
   */
  maxUpstreamRequestsPerCall: 3,
  /** سقف تلاش برای گرفتن JSON معتبر (زیرمجموعه‌ی سقف بالا) */
  maxJsonAttempts: 2,
  /**
   * سقف پیش‌فرض توکن خروجی.
   *
   * از ۲۰۰۰ بالا آمد چون سقف تنگ **ضدصرفه‌جویی** است: خروجی بریده،
   * `extractJson` یا Zod را می‌شکند و یک تلاش کامل دیگر می‌خورد. یعنی برای
   * صرفه‌جویی در توکن، یک درخواست کامل خرج می‌شد.
   */
  defaultMaxOutputTokens: 2_600,
} as const;

/* ── گاردریل جست‌وجوی وب (Tavily) ──────────────────────────── */

/**
 * Tavily کوتای رایگان محدودی دارد (~۱۰۰۰ اعتبار/ماه؛ advanced = ۲ اعتبار).
 * حالت «متوسط» انتخاب مالک: ~۲۰ اعتبار در هر کشف → حدود ۵۰ کشف در ماه.
 *
 * - maxSearchesPerRun: حداکثر فراخوان Tavily در هر دکمه‌ی کشف (نه به‌ازای هر عبارت).
 * - maxResultsPerSearch: نتایج هر فراخوان.
 * - maxLeadsPerRun: سقف لیدهای وب که به نتیجه اضافه می‌شود.
 * - searchDepth: "advanced" — نتایج غنی‌تر و دقیق‌تر (۲ اعتبار به‌جای ۱).
 * - enrichTopLeads: چند لید برترِ توان مالی برای جست‌وجوی غنی‌سازی جدا شوند.
 */
export const WEB_SEARCH = {
  maxSearchesPerRun: 8,
  maxResultsPerSearch: 10,
  maxLeadsPerRun: 25,
  searchDepth: "advanced" as const,
  enrichTopLeads: 2,
} as const;

/* ── استخراج کانال از سایت کسب‌وکار ───────────────────────── */

/**
 * پارامترهای خواندن سایت لیدها.
 *
 * این اعداد از **اندازه‌گیری روی ۷۱ سایت واقعیِ لیدهای موجود** درآمده‌اند
 * (۱۲ مرداد ۱۴۰۵)، نه از حدس:
 *
 * - از ۷۱ سایت، ۳۹ تا اصلاً قابل‌دسترس‌اند؛ بقیه دامنه‌ی مرده یا بی‌پاسخ‌اند.
 * - توزیع زمان پاسخِ همان ۳۹ تا: p50=۳.۵s، p75=۶.۱s، p90=۷.۸s، بیشینه=۱۰.۳s.
 * - با تایم‌اوت ۶ ثانیه‌ی قبلی فقط ۲۹ تا از آن ۳۹ تا می‌رسیدند. با ۹ ثانیه
 *   ۳۷ تا. یعنی ۳ ثانیه تایم‌اوت بیشتر ≈ **۲۸٪ لید بیشتر** — ارزان‌ترین
 *   بردی که در کل جریان کشف پیدا شد.
 * - همزمانی اثر بزرگ‌تری داشت: با ۱۰ درخواست هم‌زمان فقط ۴ از ۷۱ جواب گرفت،
 *   با ۳ تا ۳۹ از ۷۱. روی لینک باریک، موازی‌سازی زیاد خودش را می‌خورد.
 *
 * هشدار: این اندازه‌گیری روی لپ‌تاپ و از پشت VPN بوده. اعداد مطلقش با Vercel
 * فرق دارد؛ چیزی که منتقل می‌شود **جهت** است نه مقدار دقیق.
 */
export const EXTRACTION = {
  /** مهلت هر صفحه (میلی‌ثانیه) */
  pageTimeoutMs: 9_000,
  /**
   * چند لید هم‌زمان استخراج شوند.
   *
   * ۴ → ۸ بر اساس اجرای زنده‌ی ۱۴ مرداد روی پروداکشن: از ۱۸ لید انتخاب‌شده،
   * `site_home:no_time` برای **۱۰** تا ثبت شد — یعنی بیش از نیمی از لیدها
   * سایتشان اصلاً باز نشد و بعد به‌خاطر «هیچ راه تماس عمومی‌ای ندارد» رد
   * شدند. علت سایتِ مرده نبود، نوبت‌بندی بود: ۱۸ لید با ۴تا-۴تا یعنی ۵ نوبت
   * × ۹ ثانیه = ۴۵ ثانیه، در حالی که فقط ۲۲ ثانیه رزرو بود.
   *
   * عدد ۴ از اندازه‌گیری روی لپ‌تاپ **پشت VPN** آمده بود (با ۱۰ هم‌زمان ۴ از
   * ۷۱ جواب گرفت، با ۳ تا ۳۹ از ۷۱). ولی کشف روی Vercel اجرا می‌شود نه اینجا،
   * و آن لینکِ باریک آنجا وجود ندارد. همان هشدار بالای این بلوک: از آن
   * اندازه‌گیری فقط **جهت** منتقل می‌شود، نه مقدار.
   */
  concurrency: 8,
  /**
   * بودجه‌ی زمانی کل کشف.
   *
   * ⚠️ این عدد **قابل‌برداشتن نیست**. `maxDuration = 60` در
   * `src/app/api/discovery/route.ts` سقف سختِ پلتفرم است؛ بدون بودجه، تابع
   * وسط کار کشته می‌شود و چون درج لیدها و ذخیره‌ی مکان‌نما در انتهای جریان
   * است، **همه‌چیز از دست می‌رود** — نه لیدی ذخیره می‌شود نه پیشرفتی.
   *
   * ۴۵ → ۵۲ ثانیه: ۸ ثانیه‌ی باقی‌مانده تا سقف، عمداً برای کاری است که *بعد*
   * از مهلت اجرا می‌شود و بودجه گاردش نمی‌کند — درج سریالی لیدها
   * (findLeadByDedupKey + createLead برای هرکدام) به‌علاوه‌ی ذخیره‌ی مکان‌نما و
   * دو رکورد لاگ.
   */
  budgetMs: 52_000,
  /**
   * سهم تضمین‌شده‌ی استخراج از بودجه (منابع فقط تا بقیه‌اش وقت دارند).
   *
   * ۲۲ → ۳۰ ثانیه، و کل افزایشِ بودجه به همین‌جا رفت: سهم منابع عملاً ثابت
   * ماند (۲۳ ← ۲۲ ثانیه) چون جست‌وجوی وب تنها منبعی است که الان نتیجه می‌دهد
   * و کم‌کردن وقتش مستقیماً لید کم می‌کند. با ۳۰ ثانیه و همزمانی ۸، سه نوبت
   * کامل جا می‌شود (۲۴ لید) که از سقف روزانه‌ی ۲۰ بیشتر است.
   */
  reserveMs: 30_000,
  /** مسیر جایگزین فقط اگر دست‌کم این‌قدر از مهلت مانده باشد */
  altHostMinRemainingMs: 14_000,
} as const;

/* ── کشف: کاشی‌بندی OpenStreetMap ─────────────────────────── */

/**
 * چرا کاشی‌بندی و نه offset: Overpass QL اصلاً OFFSET/SKIP ندارد. `out … N`
 * همیشه همان N عنصر اول را به ترتیب canonical می‌دهد — یعنی هر اجرا دقیقاً
 * همان مجموعه. تنها راهِ عوض‌کردن *مجموعه*، عوض‌کردن bbox است.
 *
 * grid=۴ یعنی ۱۶ کاشی؛ با ۳ کاشی در هر اجرا کل شهر در ~۶ بار زدن دکمه پوشش
 * داده می‌شود و بعد دور بعدی شروع می‌شود.
 */
export const OSM = {
  tileGrid: 4,
  tilesPerRun: 3,
  /**
   * مهلت هر کاشی — هم در سرآیند `[timeout:N]` خود Overpass و هم در
   * AbortController سمت ما. این دو باید یکی باشند؛ قبلاً سرآیند ۳۰ ثانیه بود
   * و کلاینت هم ۳۰ ثانیه صبر می‌کرد، یعنی یک کوئری کند تنهایی دوسوم بودجه‌ی
   * ۴۵ ثانیه‌ای کشف را می‌خورد. کاشی کوچک معمولاً ۲ تا ۵ ثانیه جواب می‌دهد.
   */
  tileTimeoutMs: 12_000,
  /** سقف نتایج هر کاشی */
  maxPerTile: 120,
  /** مهلت Nominatim — فقط اجرای اول هر کمپین؛ بعدش bbox از مکان‌نما می‌آید */
  geocodeTimeoutMs: 8_000,
} as const;

/* ── کشف: صفحه‌بندی Google Places ─────────────────────────── */

export const PLACES = {
  /** هر صفحه ۲۰ نتیجه؛ سقف صفحه در هر اجرا */
  maxPagesPerRun: 3,
} as const;

/* ── کشف: Google Programmable Search ──────────────────────── */

/**
 * منبع سوم کشف. سهمیه‌ی رایگان Custom Search JSON API روزی ۱۰۰ کوئری است، پس
 * سقف هر اجرا عمداً پایین است: کشف باید چند بار در روز قابل زدن باشد.
 * عمق از **جای شروع در نقشه‌ی جست‌وجو** می‌آید نه از تعداد فراخوان.
 */
export const GOOGLE_SEARCH = {
  maxSearchesPerRun: 8,
  /** هر فراخوان حداکثر ۱۰ نتیجه می‌دهد (سقف خود API) */
  maxResultsPerSearch: 10,
  maxLeadsPerRun: 25,
  timeoutMs: 12_000,
} as const;

/* ── کشف: جمع‌آوری نامزد و پیش‌رتبه‌بندی ───────────────────── */

export const DISCOVERY = {
  /**
   * چند برابرِ سقف روزانه نامزد جمع شود.
   *
   * چرا لازم است: از این پس فیلترِ «این را از قبل می‌شناسیم» **پیش از**
   * `slice(0, limit)` اجرا می‌شود. اگر فقط `limit` نامزد جمع کنیم، بعد از فیلتر
   * ممکن است صفر تازه بماند. ۴ برابر یعنی حتی اگر ۷۵٪ تکراری باشند سهمیه پر شود.
   */
  candidateMultiplier: 4,
  /**
   * اندازه‌ی تکه در کوئری «کدام کلیدها موجودند».
   *
   * PostgREST فیلتر `in.(…)` را در query-string می‌فرستد؛ ۸۰ کلید ≈ ۴ کیلوبایت
   * URL. بدون تکه‌کردن، یک کشف پربار به سقف طول URL می‌خورد و با خطای نامفهوم
   * شبکه می‌ایستد.
   */
  dedupKeyChunk: 80,
  /**
   * وزن‌های پیش‌رتبه‌بندی نامزدها.
   *
   * جایگزین `roughAffluence` قبلی که عملاً دوتایی بود: OSM و جست‌وجوی وب هر دو
   * `rating` و `reviewsCount` را `null` می‌گذارند، پس آن تابع فقط «سایت دارد؟»
   * را می‌دید و همه ۸ یا ۰ می‌گرفتند. با مرتب‌سازی پایدار V8، ترتیب هر اجرا
   * مو‌به‌مو یکسان می‌شد — و همین ترتیب تصمیم می‌گرفت کدام ۲۰ تا از ۸۰ نامزد
   * زنده بمانند.
   *
   * این وزن‌ها فقط روی سیگنال‌هایی‌اند که در داده‌ی واقعی وجود دارند. امتیاز
   * نظرات و امتیاز گوگل حذف نشده‌اند، فقط دیگر تنها سیگنال نیستند؛ اگر روزی
   * کلید Google Places اضافه شود خودبه‌خود دوباره وزن پیدا می‌کنند.
   */
  preRank: {
    ownDomain: 8,
    freeHostDomain: 2,
    phone: 4,
    perSeedChannel: 3,
    maxSeedChannels: 2,
    instagram: 3,
    address: 2,
    scaleKeyword: 5,
    affluentArea: 4,
    reviewsDivisor: 10,
    reviewsCap: 20,
    highRating: 4,
    highRatingAt: 4.3,
  },
} as const;

/* ── امتیازدهی لید (۷ معیار وزنی — نقشه‌راه §11.4) ─────────── */

export const SCORING_WEIGHTS = {
  marketFit: 20,
  visibleNeed: 20,
  infoCredibility: 15,
  onlineActivity: 10,
  portfolioFit: 15,
  decisionMakerAccess: 10,
  lowRisk: 10,
} as const;

export type ScoringCriterion = keyof typeof SCORING_WEIGHTS;

export const SCORE_THRESHOLDS = {
  /** ≥ این امتیاز → واجد شرایط پیام */
  pass: 70,
  /** بین nurture و pass → نگه‌داری برای بعد */
  nurture: 50,
} as const;

/* ── دروازه‌ی توان مالی (پیش از تحلیل — صرفه‌جویی سهمیه) ───── */

/**
 * لیدی که نمره‌ی توان مالی‌اش زیر این حد است **تحلیل نمی‌شود**.
 *
 * چرا: سهمیه‌ی مدل رایگان ۵۰ درخواست در روز است و هر لید ردشده یک فراخوان
 * تحلیل را می‌سوزاند. غربال رایگان قبل از خرج توکن، همان اصل «فیلتر رایگان
 * پیش از مراحل توکن‌بر» نقشه‌راه است.
 *
 * عدد ۳۵ از داده‌ی واقعی کالیبره شده، نه حدس: روی ۴۵ لید موجود، توزیع نمره
 * ۰ تا ۷۲ است و آستانه‌ی ۳۵ حدود ۴۴٪ لیدها را نگه می‌دارد. نقطه‌ی شکست طبیعیِ
 * بعدی ۴۰ است که به ۲۷٪ می‌رسد — سخت‌گیرانه‌تر ولی کلینیک‌های واقعیِ دارای
 * سایت و تلفن ثابت را هم کنار می‌گذارد.
 *
 * لید زیر آستانه پاک نمی‌شود؛ وضعیتش LOW_VALUE می‌شود و همیشه می‌توان با
 * دکمه‌ی «تحلیل لید» همان ردیف دستی تحلیلش کرد (اراده‌ی صریح انسان بر دروازه
 * مقدم است).
 */
/**
 * ⚠️ از ۳۵ به ۰ آمد — یعنی **دروازه پیش‌فرض خاموش است**.
 *
 * چرا: خواسته‌ی صریح مالک بعد از کار با سیستم. دروازه در عمل بیشتر از آنکه
 * سهمیه نگه دارد، جلوی کار را می‌گرفت — لیدها بی‌صدا `LOW_VALUE` می‌شدند، بعد
 * دکمه‌ی «تولید پیام برای همه» به‌خاطر همان‌ها غیرفعال می‌ماند و از دید مالک
 * «دکمه خراب است».
 *
 * صرفه‌جویی واقعی جای دیگری بسته شد: `AI_LIMITS.maxUpstreamRequestsPerCall`
 * که تا امروز هیچ سقفی نداشت و تا ۶ درخواست برای یک فراخوان می‌زد. گارد در جای
 * درست، نه در مسیر کار کاربر.
 *
 * برای روشن‌کردن دوباره کافی است این عدد را بالا ببری؛ کل منطق دروازه سر جایش
 * است. عدد ۳۵ از داده‌ی واقعی کالیبره شده بود: روی ۴۵ لید موجود، توزیع نمره
 * ۰ تا ۷۲ بود و آستانه‌ی ۳۵ حدود ۴۴٪ لیدها را نگه می‌داشت.
 */
export const AFFLUENCE_THRESHOLDS = {
  analyze: 0,
} as const;

/* ── گاردهای خط تولید ─────────────────────────────────────── */

export const PIPELINE_GUARDS = {
  /**
   * قفل همزمانی: چقدر یک لید در وضعیت ANALYZING «در حال اجرا» حساب شود.
   *
   * از ۳ دقیقه کوتاه شد. اگر اجرا بیرون از پروسه بمیرد، rollback اجرا نمی‌شود
   * و لید تا پایان این مهلت یخ می‌ماند — یعنی گاردِ صرفه‌جویی خودش کار را
   * متوقف می‌کرد. ۴۵ ثانیه کمی بیشتر از سقف ۶۰ ثانیه‌ای Vercel نیست، پس اجرای
   * سالم را قطع نمی‌کند.
   */
  staleLockMs: 45_000,
  /**
   * سقف گام در حلقه‌های سمت کلاینت (تحلیل/پیامِ یک لید).
   *
   * از ۸ آمد بالا. بدترین حالت واقعی دقیقاً ۹ گام است (تحلیل، انتخاب خدمت،
   * نمونه‌کار، پیش‌نویس، نقد، بازنویسی، نقد، بازنویسی، نقد) — یعنی سقف قبلی
   * یکی کم بود و لید در گام آخر بی‌صدا رها می‌شد در حالی که UI «کامل شد»
   * اعلام می‌کرد.
   */
  maxClientSteps: 12,
} as const;

/* ── روبریک نقد پیام (نقشه‌راه §23) ────────────────────────── */

export const CRITIC_RUBRIC = {
  accuracy: 20, // صحت و اتکا به Evidence
  personalization: 15, // شخصی‌سازی مختص همان بیزینس
  clarity: 15,
  naturalness: 10,
  serviceFit: 10,
  cta: 10,
  constraints: 20, // رعایت محدودیت‌ها (بدون قیمت/ادعا/…)
} as const;

export const CRITIC_THRESHOLDS = {
  /** ≥ این → تأیید */
  pass: 85,
  /** بین revise و pass → یک دور اصلاح */
  revise: 75,
} as const;

/** حداکثر دور بازنویسی پیام؛ بعد از آن → Human Review */
export const MAX_REVISION_ROUNDS = 2;

/**
 * گاردریل حلقه‌ی بی‌نهایت (نقشه‌راه §14/§32): سقف گام‌های ارکستریتور برای هر
 * لید در یک اجرا. اگر پر شد، اجرا با stop_reason=MAX_STEPS متوقف می‌شود.
 */
export const MAX_STEPS_PER_LEAD = 10;

/* ── قواعد پیام (نقشه‌راه §22 + دستور پیام‌نویسی مالک) ─────── */

/**
 * طول پیام **تابع کانال** است، نه یک عدد ثابت.
 *
 * چرا: دایرکت اینستاگرام جای پیام کوتاه است و متن ۱۲۰ کلمه‌ای آنجا خوانده
 * نمی‌شود؛ ایمیل برعکس، جا برای توضیح دارد. عدد ثابت قبلی (۸۰–۱۲۰) باعث
 * می‌شد نویسنده پیام دایرکت را بی‌دلیل کش بدهد.
 *
 * این‌ها **سقف قطعی نیستند**: کوتاه‌ترین نسخه‌ای که منظور را کامل می‌رساند
 * بهتر است. Policy Guard فقط خارج‌از‌بازه‌بودن را به‌عنوان هشدار ثبت می‌کند،
 * نه به‌عنوان BLOCK.
 */
export const MESSAGE_LENGTH_BY_CHANNEL: Record<ChannelKey | "email", { min: number; max: number }> = {
  instagram: { min: 50, max: 90 },
  whatsapp: { min: 60, max: 110 },
  telegram: { min: 60, max: 110 },
  siteForm: { min: 60, max: 110 },
  phone: { min: 60, max: 110 },
  email: { min: 80, max: 140 },
};

/** بازه‌ی طول برای یک کانال (اگر کانال نامشخص بود، حالت میانه) */
export function messageLengthFor(channel: ChannelKey | "email" | null): { min: number; max: number } {
  return (channel && MESSAGE_LENGTH_BY_CHANNEL[channel]) || { min: 60, max: 110 };
}

/**
 * حداکثر خدمت در یک پیام: ۱ اصلی + تا ۲ مکمل — به‌شرطی که هر سه **یک نتیجه‌ی
 * مشترک** بدهند. فهرست‌کردن توانایی‌ها ممنوع است.
 */
export const MAX_SERVICES_PER_MESSAGE = 3;

/** حداکثر نمونه‌کار پیشنهادی برای اتچ — معمولاً ۱ */
export const MAX_PORTFOLIO_PER_MESSAGE = 2;

/** ترتیب اولویت انتخاب نمونه‌کار (دستور پیام‌نویسی، بند ۸) */
export const PORTFOLIO_MATCH_PRIORITY = [
  "همان صنعت",
  "مسئله‌ی مشابه",
  "سبک بصری نزدیک",
  "نوع خروجی مشابه",
  "نزدیک‌ترین نمونه‌ی تأییدشده",
] as const;

/* ── اولویت کانال ارتباط ──────────────────────────────────── */

/**
 * اولویت انتخاب کانال مجازی برای ارسال. ایمیل جداست: اگر لید ایمیل داشته
 * باشد، همیشه یک نسخه‌ی ایمیلی هم علاوه بر کانال مجازی ساخته می‌شود.
 */
export const CHANNEL_PRIORITY: ChannelKey[] = [
  "instagram",
  "whatsapp",
  "telegram",
  "siteForm",
  "phone",
];
